// 'use client';

/*
type DashboardType = {
  // children: ReactNode;
}
*/

import DivContainer from '@/components/UI/DivContainer';

export default function Dashboard(/*{}: DashboardType*/) {
  return (
    <DivContainer>
      Dashboard
    </DivContainer>
  );
}
